services:
  mongo:
    image: mongo:7
    container_name: chat_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - chat_mongo_data:/data/db

volumes:
  chat_mongo_data:
